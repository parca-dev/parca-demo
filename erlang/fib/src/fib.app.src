%% Feel free to use, reuse and abuse the code in this file.

{application, fib, [
	{description, "Parallel Fibonacci example."},
	{vsn, "1.0.0"},
	{modules, []},
	{registered, [fib]},
	{applications, [
		kernel,
		stdlib
	]},
	{mod, {fib, []}},
	{env, []}
]}.
